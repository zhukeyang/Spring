3.4相对路径
    在页面中有路径问题，访问路径有"/"开头的，还有没有"/"的。
    地址的区别，现在看的都是在页面中的地址
    1.有协议开头的如：http://www.baidu.com，称为绝对地址。地址是唯一的，能够直接访问。
    2.没有协议开头的，例如test/some.do，称为相对路径，相对路径单独使用不能表示某个资源，不能访问。
    相对地址必须有参考地址在一起，才能表示一个完整的资源，才可以访问

    参考地址：有"/"和没有"/"的参考地址是不同的
    1.没有斜杠的地址，参考地址：当前资源的访问路径
    解决方式：
        1.加EL表达式${pageContext.request.contextPath}(表示访问项目的路径)
            优点：好理解
            缺点：每个链接地址，都需要加EL表达式
        2.固定当前页面中的 没有"/"开头的:加base标签
    2.有斜杠开头的地址：参考地址是服务器地址，也就是从协议开始到端口号位置
    解决方法：forward+完整视图路径



4.2异常处理：
    1.@ExceptionHandler:放在方法上面，表示此方法可以处理某个类型的异常，当异常发生时，执行这个方法。
    2.@ControllerAdvice:放在类的上面，表示这个类中有异常的处理方法，相当于aop中的@Aspect
      @ControllerAdvice看做是 控制器增强，给Controller类增加异常（切面）的处理功能
